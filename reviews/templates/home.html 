<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business Reviews</title>
    <link rel="stylesheet" href="{% static 'css/styles.css' %}">  <!-- Link to your CSS file -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-5">
    <h1 class="text-center">Business Reviews</h1>
    <div class="row">
        {% for business in businesses %}
            <div class="col-md-4">
                <div class="card mb-4">
                    <img src="{{ business.qr_code.url }}" class="card-img-top" alt="QR Code for {{ business.name }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ business.name }}</h5>
                        <p class="card-text">Location: {{ business.city }}, {{ business.location }}</p>
                        <p class="card-text">Phone: {{ business.phone }}</p>
                        <a href="{% url 'business_qr' business.id %}" class="btn btn-primary">Scan QR Code</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
